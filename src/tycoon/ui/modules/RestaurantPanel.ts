/**
 * Restaurant module panel: client-only for now. Restaurant open/close state on server (profile.restaurantOpen).
 * Stub server hook: /open restaurant (optional).
 */

export const RESTAURANT_MODULE_ID = 'RESTAURANT' as const;
