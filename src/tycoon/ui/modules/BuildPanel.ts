/**
 * Build module panel: server-side stub. Panel UI is in client (assets/ui).
 * BUILD module requires plot; opening requests server enter build mode.
 */

export const BUILD_MODULE_ID = 'BUILD' as const;
